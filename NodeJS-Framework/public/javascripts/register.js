

var config = {};
function trim(str) {
    var result = str;
    result = str.replace(/(\s+)/ig, ' ');
    result = result.replace(/^\s+/ig, '');
    result = result.replace(/\s+$/ig, '');
    return result;
}
